name: nodeguard_personnel_app
description: NodeGuard Personnel application for La Trinidad MDRRMO responders.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  connectivity_plus: ^7.3.0
  shared_preferences: ^2.5.5
  supabase_flutter: ^2.15.4
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/mdrrmc-logo.png
